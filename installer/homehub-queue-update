#!/usr/bin/env bash
set -euo pipefail
VERSION="${1:-}"
[[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "Invalid version" >&2; exit 2; }
exec /usr/bin/systemd-run --unit="homehub-update-${VERSION//./-}" --property=Type=exec /usr/local/sbin/homehub-apply-update --version "$VERSION"
